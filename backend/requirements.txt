# Pinned to latest stable as of scaffold (2026-06). Re-lock with `pip list --outdated`.
# Kept lean (plain fastapi/uvicorn) for fast, reliable builds on Python 3.14.

# Web framework / ASGI server
fastapi==0.138.0
uvicorn>=0.35.0

# Data validation / settings
pydantic>=2.11.0
pydantic-settings>=2.7.0

# HTTP + WebSocket client for Home Assistant
httpx>=0.28.0
websockets>=14.0

# Scheduling
APScheduler>=3.11.0

# Storage (async SQLite)
SQLAlchemy[asyncio]>=2.0.36
aiosqlite>=0.20.0

# Config + time
PyYAML>=6.0.2
python-dateutil>=2.9.0

# Local admin login (bcrypt password verification)
bcrypt>=4.2.0

# --- Optional extras (Phase 3/4). Uncomment to enable. ---
# numpy>=2.1.0            # required by ML load forecasting
# pandas>=2.2.0
# scikit-learn>=1.6.0     # Phase 4: ML load forecasting
# pulp>=2.9.0             # Phase 3: MPC optimizer
# ortools>=9.11.0
