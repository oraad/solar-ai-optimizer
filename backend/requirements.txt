# Python 3.14+ required. Re-lock: docker compose build test && docker compose run --rm --entrypoint pip test freeze
# Kept lean (plain fastapi/uvicorn) for fast, reliable builds on Python 3.14.

# Web framework / ASGI server
fastapi==0.139.0
uvicorn>=0.49.0

# Data validation / settings
pydantic>=2.13.4
pydantic-settings>=2.14.2

# HTTP + WebSocket client for Home Assistant
httpx>=0.28.1,<1.0
websockets>=16.0

# Scheduling
APScheduler>=3.11.2

# Storage (async SQLite)
SQLAlchemy[asyncio]>=2.0.51,<2.1
aiosqlite>=0.22.1

# Config + time
PyYAML>=6.0.3
python-dateutil>=2.9.0.post0

# Local admin login (bcrypt password verification)
bcrypt>=5.0.0

# --- Optional extras (Phase 3/4). Uncomment to enable. ---
# numpy>=2.5.0            # required by ML load forecasting
# pandas>=2.2.0
# scikit-learn>=1.9.0     # Phase 4: ML load forecasting
# pulp>=3.3.2             # Phase 3: MPC optimizer
# ortools>=9.11.0
